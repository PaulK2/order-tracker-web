{
  "name": "cdc-order-workspace",
  "version": "2.1.0",
  "private": true,
  "description": "CDC product knowledge and personal order workspace",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.cjs",
    "test:ocr": "node tests/ocr-integration.cjs",
    "check": "node --check app.js && node --check data.js && node --check ocr.js && node --check ocr-parser.js && node --check tracking.js"
  },
  "devDependencies": {
    "fake-indexeddb": "6.0.0",
    "jsdom": "26.1.0",
    "tesseract.js": "7.0.0"
  }
}
